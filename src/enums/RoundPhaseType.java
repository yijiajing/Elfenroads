package enums;

public enum RoundPhaseType {
    DEAL_CARDS,
    DEAL_HIDDEN_COUNTER,
    DRAW_COUNTER_ONE, DRAW_COUNTER_TWO, DRAW_COUNTER_THREE,
    PLAN_ROUTES_ONE, PLAN_ROUTES_TWO, PLAN_ROUTES_THREE, PLAN_ROUTES_FOUR, PLAN_ROUTES_FIVE, PLAN_ROUTES_SIX,
    MOVE,
    RETURN_COUNTERS
    // removed AUCTION for now because it is not part of this version yet
}
